# Polyseat - seat session (headless)
#
# Generated by polyseatd. Edits are overwritten the next time the seat is
# provisioned.
#
# A single virtual output. Sunshine renegotiates resolution and refresh rate
# per connected client; this is what the session comes up with.
output HEADLESS-1 resolution {{.Resolution}} position 0,0

# Sunshine only learns WAYLAND_DISPLAY once sway has opened the socket.
exec systemctl --user import-environment WAYLAND_DISPLAY XDG_SESSION_TYPE
exec systemctl --user start polyseat-sunshine.service

# Something visible, so the first stream is not just a black screen.
exec foot
