# polyseat — Sunshine im Seat
# wlr-Capture ist auf NVIDIA der einzige gangbare Weg; KMS-Capture
# funktioniert mit dem proprietären Treiber nicht.
capture = wlr
encoder = nvenc
audio_sink = polyseat
origin_web_ui_allowed = lan
